# Command Line Tool to deploy Skopos to your AWS Account

`npm build -w packages/cli`
`npm install -g .`
- `skopos deploy`  (deploy skopos to your AWS Account)
- `skopos destroy` (destroy all AWS resources)